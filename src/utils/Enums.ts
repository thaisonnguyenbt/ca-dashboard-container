export enum LogLevel {
  TRACE = 'TRACE',
  DEBUG = 'DEBUG',
  INFO = 'INFO',
  WARN = 'WARN',
  ERROR = 'ERROR',
  FATAL = 'FATAL',
}

export enum PromiseStatus {
  FULFILLED = 'fulfilled',
  REJECTED = 'rejected',
}
